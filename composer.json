{
    "name": "ivanfuhr/essentials",
    "description": "Just better defaults for your Laravel projects.",
    "keywords": ["php", "laravel", "package", "essentials", "laravel-essentials"],
    "license": "MIT",
    "authors": [
        {
            "name": "Ivan Führ",
            "email": "dev.ivanfuhr@gmail.com"
        }
    ],
    "require": {
        "php": "^8.3.0",
        "laravel/framework": "^11.44.2|^12.52.0|^13.5",
        "monolog/monolog": "^3.6"
    },
    "require-dev": {
        "laravel/pint": "^1.29.0",
        "orchestra/testbench": "^9.0|^10.9.0|^11.1",
        "pestphp/pest": "^3.8.5|^4.6.2",
        "pestphp/pest-plugin-laravel": "^3.2.0|^4.0.0",
        "pestphp/pest-plugin-type-coverage": "^3.6.1|^4.0.4",
        "phpstan/phpstan": "^2.1.50",
        "rector/rector": "^2.4.2",
        "symfony/var-dumper": "^7.4.4|^8.0.8"
    },
    "autoload": {
        "psr-4": {
            "IvanFuhr\\Essentials\\": "src/"
        },
        "files": [
            "src/helpers.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "sort-packages": true,
        "preferred-install": "dist",
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "scripts": {
        "refactor": "rector",
        "lint": "pint --parallel",
        "test:refactor": "rector --dry-run",
        "test:lint": "pint --parallel --test",
        "test:types": "phpstan analyse --ansi --memory-limit=512M",
        "test:unit": "pest --colors=always --parallel",
        "test:coverage": "pest --colors=always --coverage --parallel --min=100",
        "test": [
            "@test:refactor",
            "@test:lint",
            "@test:types",
            "@test:unit"
        ]
    },
    "extra": {
        "laravel": {
            "providers": [
                "IvanFuhr\\Essentials\\EssentialsServiceProvider"
            ]
        }
    }
}
